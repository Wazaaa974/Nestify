<div class="container">

  <div class="text-package text-center mt-3">
    <h1><%= @package.name %></h1>
  </div>

  <div class="text-center mt-3 mb-3">
    <p>
      Dans ce package vous sont proposés les produits essentiels à une ambiance qui vous ressemble. Vous pouvez personnaliser votre package en ajoutant, remplaçant ou supprimant des produits. C'est votre univers, c'est vous qui décidez !
    </p>
  </div>

<div class="button-container d-flex justify-content-center gap-3 mt-1 mb-3">
  <%= link_to packages_path, class: "btn btn-retour" do %>
    Les packages
  <% end %>

  <%= link_to dashboards_path, class: "btn btn-dashboard" do %>
    Favoris
  <% end %>

   <%= render 'shared/favorite_button', proposal: @proposal %>
</div>


  <div class ="mt-4">
    <h3 id = "package-<%= @package.id%>-budget" class="budget"> Budget total : <%= @sum.round %>€ </h3>
    <%= turbo_stream_from "package-#{@package.id}-budget" %>
    <%# mettre le lien du bouton pour enregistrer en favoris avec alerte %>
  </div>

  <hr class = "custom-hr">


  <div id = "packages" >
    <% @package.products.each do |product| %>
     <%= render "product", product: product, package: @package %>
    <% end %>
  </div>

  <div class= "d-flex justify-content-center align-items-center">
  <%# ajouter un produit %>
    <%= link_to packages_path, class: "btn btn-add-product mt-4" do %>
      <i class="fa-solid fa-plus"></i>
    <% end %>
  </div>
</div>
